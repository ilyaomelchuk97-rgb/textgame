# Кости и Судьбы — отчёт за раунд v22: «Темы стали материалами»

24 сентября 2026 · сборка `game.html` — 1222 КБ · тесты **126/126** · проверено в живом браузере на iPhone 13 mini

---

## 1. Что просили

> «При изменении оформления меняется не только цвет фона, но и вид всего: кнопок, окон и т.д.
> Если, например, выбран лёд — то кнопки и блоки обледенели и покрыты льдом».

---

## 2. Что сделано

Раньше тема меняла только палитру. Теперь **у каждой темы свой материал интерфейса**: форма углов,
кромка, заливка, блик, тень — у кнопок, полос, карточек, окон, тостов и шапки. Разметка при этом
одна и та же: скины меняют вид, но **не отступы, не высоту и не размер шрифта** — поэтому раскладка
осталась прежней (шапка 14 %, картинка 22 %, кнопка 44+ px).

### 2.1. Девять материалов

| Тема | Из чего сделана |
|---|---|
| 🩰 **Лёд** | кнопки и блоки обледенели: многослойная наледь по верхнему краю (15 слоёв), иней в нижних углах, морозные лучики по поверхности; над кадром сцены и над полосой вариантов висят **сосульки**, сам кадр подёрнут инеем по краям; подписи на ледяных кнопках — холодно-синие, как тень на снегу |
| 🌃 **Киберпанк** | острые углы (4 px), неоновая кромка, **сканлайны** по кнопкам и блокам, свечение текста |
| 💻 **Терминал** | зелёный фосфор, **пунктирные рамки** (единственная тема с пунктиром), сканлайны и ровные строки консоли |
| 📜 **Пергамент** | бумажные листы на тёмном столе: у блока свой **неровный край** (разные радиусы углов), зернистость бумаги, мягкая тень; текст на кнопках — чернильный, а не белый |
| ⚒️ **Тушь и медь** | прямые углы, медная кромка и **заклёпки по четырём углам** блоков (14 слоёв фона) |
| 🌇 **Закат** | тёплое свечение и градиенты, крупные мягкие формы (22 px) |
| Материальная | плоские карточки **без рамок**, вместо кромки — тень, как и положено Material |
| Чёрная | только тонкий контур на чистом чёрном: ни градиентов, ни свечения, ни тени |
| Ночь | тёмное стекло с мягким бирюзовым кантом |

### 2.2. В настройках видно, что выбираешь

Рядом с названием каждой темы — **образец материала**: маленькая карточка с кнопкой внутри,
в её собственной фактуре (все десять разные). Под списком — расшифровка словами: «Тема »Лёд«:
обледеневшие кнопки и блоки: наледь, иней, сосульки по кромке». Переключение меняет материал
сразу, не выкидывая из настроек.

### 2.3. Как это устроено (чтобы дальше было просто добавлять темы)

* `src/skins.css` — новый файл: каждая тема задаёт токены `--sk-*` (радиус, заливка, блик, тень,
  свечение), а общий блок применяет их к кнопкам, чипам, вкладкам, полосам, карточкам и окнам.
  Новая тема — это один блок токенов, а не десять переписанных селекторов.
* Значки внутри кнопок (теги проверок, шансы, подписи) больше не бледнеют на светлых темах:
  в «Пергаменте» и «Льде» они читаются тёмным/синим вместо светло-голубого.
* Скины попали и в одностраничную сборку (`build.py`), и в офлайн-оболочку (`sw.js` → `dt2-v11`).

### 2.4. Починено по пути

**Строки настроек остались без подписи.** При добавлении образцов в настройках кнопки без образца
стали пустыми — в том числе «🧭 Как держу телефон» с режимом «одной рукой». Это поймал старый
прогон `npm run touch` («режим »одной рукой« не включился»), и это же сторожит новый прогон скинов:
в настройках **62 строки, безымянных 0**.

---

## 3. Что проверено

| Проверка | Результат |
|---|---|
| `node tools/skins.js` (новый) | **22/22**: у всех 9 тем свой материал; формы от 3 px («Тушь и медь») до 20 px («Материальная»), 4 разных радиуса; пунктир только у «Терминала»; надпись читается везде (**9.8:1** в худшем случае, бледнейший слой кнопки — 5.4:1); главные кнопки читаются 9/9; ряд значков ровный в каждой теме; кнопки 44+ px; «Лёд» — наледь 15 слоёв и сосульки; «Киберпанк» — сканлайны; «Тушь и медь» — заклёпки; 10 образцов тем в настройках + расшифровка; переключение на «Лёд» меняет материал на месте; все пункты настроек подписаны (62/62) |
| `node --test tests/` | 126/126 без изменений |
| `npm run layout` | 5/5 — раскладка не поехала: 13 mini 15 %/155 px, SE 15.8 %/112 px, Pixel 5, 15 Pro Max, 360×640 |
| `npm run v20` | блок B и интерфейс на месте (темы, припасы, предметы, светлая тема живьём) |
| `npm run touch` | жесты и «одной рукой» снова работают (кнопка 54 px) — было сломано, починено |
| `npm run books` | истории без ИИ: 25/25, «Соляной тракт» дочитан до концовки |
| `npm run smoke` | весь путь игрока — ✓ |
| `npm run offline` | без сети игра открывается, оболочка `dt2-v11` |

Снимки: `shots/v22-skin-<тема>.png` для каждой из девяти тем, `shots/v22-skins.png` — общий лист
всех девяти на одном фоне (собирает `tools/skins-sheet.py`), `shots/v22-skins-settings.png` —
раздел «Оформление» с образцами тем, `shots/v22-skins-ice-settings.png` — переключение на «Лёд».

---

## 4. Что изменилось в файлах

| Файл | Что сделано |
|---|---|
| `src/skins.css` | **новый файл**: токены `--sk-*` на каждую тему, применение к кнопкам, полосам, карточкам и окнам, наледь и сосульки «Льда», образцы `.skin-demo` |
| `src/app.js` | `choiceRow` умеет образец материала; подписи тем описывают материал; под списком — расшифровка выбранной темы |
| `index.html`, `build.py`, `sw.js` | скины подключены в странице, в сборке и в офлайн-кэше (`dt2-v11`) |
| `package.json` | версия 22.0.0, прогон `npm run skins` в общем списке |
| `tools/skins.js`, `tools/skins-sheet.py` | новый прогон девяти материалов и лист снимков |
| `README.md` | раздел «Что нового в v22», строка в структуре, проверка |

---

## 5. Что осталось

* `tools/frame.js` — кадр не поднимается из IndexedDB после перезапуска (старое).
* `npm run cloud-check` — падает с ошибкой самого npm (старое, не разобрано).
* Пункт 37 «Ежедневный забег» из PLAN.md — не начат (старое).
* `tools/win-local.js` — при полном обрыве всех запросов к `/api/*` экран героя не уходит в игру
  (стартовый вопрос мастера ждёт ответа без таймаута). Обычный офлайн работает.
