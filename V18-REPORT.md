# v18 — ключ Hugging Face: 137 моделей ведущего и очередь кадров

**Версия:** 18.0.0 · сборка `game.html` 973 КБ · тесты 106/0

---

## 1. Что показал ключ (проверял живьём)

| Проверка | Результат |
|---|---|
| Ключ живой? | **Да.** Профиль `IlyaInterdust`, токен «textgame», право `inference.serverless.write` |
| Текст | **Работает: роутер Inference Providers отдаёт 137 моделей** у 14 провайдеров (featherless, groq, novita, together, scaleway…). Ход через `zai-org/GLM-5.3-Flash` — валидный русский JSON за **1.8–2.7 с**, `deepseek-ai/DeepSeek-V4.1-Flash` — за 1.5 с |
| Кредиты бесплатного аккаунта | **Крошечные и заканчиваются.** В одной серии из 12 запросов роутер отдал `402 «You have depleted your monthly included credits»`; позже канал снова отвечал. Игра должна уметь оба состояния — и умеет |
| Картинки через платных провайдеров HF (fal-ai, replicate, nscale…) | **Недоступны** этому аккаунту: `400 «Model not supported by provider»` — им нужны предоплаченные кредиты или PRO |
| Картинки через открытые Space'ы | **Работают, и ключ им идёт на пользу:** `flux-merged` — 5.4 с, `sd-3.5-large` — 8.9 с (кадр 29 КБ). Анонимную квоту Space'ы режут чаще, чем именную |

Попутно нашлась поломка, которая была бы у игрока всегда: Space `FLUX.1-schnell` (генератор в игре) **отдаёт 404 изнутри** — кадр не мог выйти никогда, время тратилось впустую. Заменил на живой `FLUX.1-dev`.

## 2. Что сделано в игре

* **Новый ведущий — «Hugging Face (137 моделей)»** в «Настройки → Канал ИИ». Цепочка `GLM-5.3-Flash → DeepSeek-V4.1-Flash → Qwen3.8-27B`, удачная модель запоминается на 10 минут, ответ идёт потоком, имя модели видно в логах: `hf:zai-org/GLM-5.3-Flash`.
* **Тот же ключ идёт в картинки**: запрос кадра уходит в Space с `Authorization: Bearer hf_…`.
* **Пустые кредиты не тормозят игру:** роутер ответил 402 — канал понимает это за **40 мс**, молча уступает другому мастеру, а `/api/health` показывает причину (`hf.credits`). Свой ключ игрока с кредитами пробуется всегда — даже когда вшитый выработан.
* **Поле «ключ Hugging Face (hf_…)»** в настройках: ключ остаётся в телефоне, как у Mistral и GLM. Переменные сервера: `HF_API_KEY` (или `HUGGING_FACE_TOKEN`), `HF_BASE_URL`, `HF_MODEL_CHAIN`, `HF_BUILTIN_KEY=""` выключает вшитый ключ.
* Ключ в публичную сборку **не попадает** (проверено: 0 совпадений).

## 3. Живые прогоны

| Проверка | Что показала |
|---|---|
| `npm run hf` (моки роутера и Space) | ведёт игру, поток кусками, кадр приходит **с ключом**, пустые кредиты — отказ за 43 мс, свой ключ из настроек работает — ❤ |
| `npm run hf:live` (настоящий ключ) | ход `hf:zai-org/GLM-5.3-Flash` за 1.8 с (сцена 704 симв., JSON разобрался), кадры `flux-merged` 5.4 с и `sd-3.5-large` 8.9 с — ❤ (`shots/v18-hf-live.txt`, кадр `shots/v18-hf-frame.png`) |
| `npm run hf:game` (браузер, iPhone 13 mini) | игрок выбрал Hugging Face пальцем в настройках → игра пошла через `hf:zai-org`, ходы 6.0 и 6.5 с, сцена связная, варианты со шансами — ❤ (`shots/v18-hf-master.png`, `shots/v18-hf-settings-picked.png`) |
| Регрессии | `npm test` 106/0 · `mistral`/`glm`/`hf` ✓ · `mistral:live` ✓ · `turns:live` ✓ · `glm:live` ✓ · `check/round/round2/screens/pack/frame/touch/layout/fonts/offline/smoke` ✓ |

## 4. Что это значит для игры

* **Ведущих теперь четыре**: шлюз, Mistral-агент, GLM и Hugging Face — плюс Pollinations и встроенный мастер. Если один канал устал, игру ведёт другой, и это видно в настройках.
* **Картинки стали честнее**: мёртвый генератор заменён, живые вызываются по имени, кадр приходит за 5–9 с (пока он рисуется, сцену показывает локальный фон).
* **Про кредиты.** Бесплатных кредитов HF хватает на считанные ходы в месяц — поэтому канал стоит запасным, а не первым. Хотите вести игру на `GLM-5.3-Flash` постоянно: либо свой ключ HF с кредитами (PRO или предоплата), либо тот же GLM напрямую — он в игре уже есть и играет бесплатной flash.

## 5. Что дальше

По плану остаётся выбор: блок **B «Механики и глубина»** (предметы с эффектами, отношения со знакомыми, состояния, социальные броски, припасы, крит с последствиями) или **37 «Ежедневный забег»** (один seed на всех, встроенный мастер, работает без ключа). Скажите, что берём.
