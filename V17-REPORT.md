# v17 — третий ведущий: GLM (Zhipu). Итог по вашему ключу

**Версия:** 17.0.0 · сборка `game.html` 971 КБ · тесты 105/0

---

## 1. Что с ключом (проверял живьём, не на словах)

| Что проверял | Результат |
|---|---|
| Ключ принят? | **Да.** `GET /models` → 200, 11 моделей: glm-4.5, glm-4.5-air, glm-4.6, glm-4.7, glm-5, glm-5-turbo, glm-5.1, glm-5.2, glm-5.3, glm-5.3-flash, glm-5.3-flashx |
| Умные модели (glm-4.6, glm-5.x) | **Нет баланса:** 429, код `1113`, «余额不足或无可用资源包,请充值» (Insufficient balance). То же на домене z.ai и на coding-эндпоинте |
| Бесплатная модель | **Работает: `glm-4.5-flash`** — 200 OK, отвечает по-русски, JSON-контракт хода держит, поток — обычный SSE |

То есть ключ живой, но **счёт пуст**: играть на нём можно только бесплатной flash. Как только счёт пополнится, умные модели подхватятся сами — они уже стоят в цепочке.

## 2. Что сделано в игре

* **Канал GLM вшит** в `server.js` (ключ формата «id.secret») — работает из коробки, как Mistral. В «Настройки → Канал ИИ» появился выбираемый ведущий **«GLM (Zhipu, бесплатная flash)»** и поле **«ключ GLM (id.secret)»** для своего ключа.
* **Цепочка моделей:** `glm-4.5-flash → glm-4.6 → glm-4.5 → glm-4.5-air → glm-5.3-flash`, с памятью удачной модели на 10 минут.
* **Пустой счёт не тормозит игру:** канал распознаёт ошибку 1113, не ждёт и не повторяет зря — сразу идёт к бесплатной flash. Если платных моделей в цепочке нет и flash недоступен, канал честно сдаётся (игра не выдумывает текст), а в `/api/health` появляется `glm.balance` с причиной.
* **Размышления выключены** (`thinking: disabled`): без этого flash тратила на них весь ответ и ход растягивался вдвое.
* **Ответ идёт потоком** и несёт имя модели: провайдер помечается как `glm:glm-4.5-flash`. То же уточнение получил и Mistral (`mistral:mistral-medium-latest`) — в логах теперь видно, кто именно ответил.
* **Порядок «Авто» не изменился:** шлюз → Mistral-агент → Mistral → **GLM** → Pollinations → встроенный мастер. Игру по умолчанию по-прежнему ведёт Mistral (4–7 с на ход), GLM — полноценный запас.
* Ключ в публичную сборку **не попадает** (проверено поиском: 0 совпадений). Переменные на сервере: `GLM_API_KEY`, `GLM_BASE_URL`, `GLM_MODEL_CHAIN`, `GLM_BUILTIN_KEY=""` выключает вшитый ключ.

## 3. Живые прогоны

| Проверка | Что показала |
|---|---|
| `npm run glm` (мок) | бесплатная flash ведёт игру, поток на месте, пустой счёт не тормозит, ключ из настроек работает — ❤ |
| `npm run glm:live` (настоящий ключ) | ходы 19.6 с и 11.1 с (главное — холодный старт), JSON разбирается, самопроверка пройдена, вариантов три со статами — ❤ (`shots/v17-glm-live.txt`) |
| `npm run glm:game` (браузер, iPhone 13 mini) | игрок **сам выбрал GLM в настройках**, игра пошла через `glm:glm-4.5-flash`, сцена связная, варианты со шансами — ❤ (`shots/v17-glm-master.png`, `shots/v17-settings-glm-picked.png`, `shots/v17-glm-browser.txt`) |
| Регрессии | `npm test` 105/0 · `mistral` ✓ · `mistral:live` ✓ · `turns:live` 4.7 с ✓ · `check/round/round2/screens/pack/frame/touch/layout/fonts/offline/smoke` ✓ |

## 4. Что это значит для игры

* **Выигрыш сразу:** есть третий независимый ведущий. Если Mistral-агент занят или ключ кончится, игра не «тупеет» до локального мастера — её берёт GLM.
* **Честная цена:** GLM медленнее Mistral-агента (11–26 с против 4–7 с на ход) — поэтому он в запасе, а не первым.
* **Если захотите умный GLM** (glm-4.6/5.x): пополнить счёт в консоли Zhipu — в игре менять ничего не нужно, цепочка сама начнёт отвечать старшими моделями, а `health.glm.balance` перестанет показывать пустой счёт.

## 5. Что дальше

Из плана `PLAN.md` осталось два пункта: блок **B «Механики и глубина»** (предметы с эффектами, отношения со знакомыми, состояния, социальные броски, припасы, крит с последствиями) и **37 «Ежедневный забег»** (один seed на всех, встроенный мастер, работает без ключа). Скажите, что берём, — или дайте другое задание.
