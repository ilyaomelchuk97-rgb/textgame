# Раунд по PLAN.md — что сделано после отчёта №1

Тема отрезка: **«кадр есть всегда»** (пак фонов, честный прогресс, офлайн-кэш кадров)
и **«всё под большой палец»** (жесты, одна рука, доступность).
Всё — без ключей и без интернета: локальный фон рисуется на телефоне за пару миллисекунд.

---

## 1. Офлайн-пак фонов и слои — пункт 13

`src/backdrop.js` вырос с 9 «абстрактных» основ до **24 узнаваемых мест**
(+15 новых, четыре переделаны):

| Было (9) | Стало (24) |
| --- | --- |
| лес, город, руины, пещера, море, пустыня, снег, космос, интерьер | + трактир, ночной рынок, порт, палуба корабля, вокзал/перрон, храм, библиотека, мастерская, крепость, дорога, мост, каньон, болото, поле боя, деревня |

**Слои поверх любой основы** (читаются из текста сцены функцией
`E.sceneLayersFromText` и уходят в промпт генератору через `E.sceneLayersPrompt`):

* время суток — `рассвет / день / закат / ночь` (перекрашивают палитру мира, а не рисунок);
* погода — `дождь, гроза (со вспышками), снег, туман, пепел, ветер, ясно`;
* костёр/очаг — тёплый свет у земли, пламя и искры.

Доводка по глазам: пещера больше не похожа на море (каменный мешок, сталактиты, свет из глубины),
у корабля появились палуба, перила, ванты, волна за бортом и фонарь, в трактире — очаг и кружки,
в библиотеке — свеча и стопка книг, в комнате — стол, стул и лампа.

Правила выбора места тоже разведены: «развалины храма» — руины (а не храм), «порт, краны» — порт,
«деревня» — деревня (а не лес и не город), «вокзал» — станция, «космическая станция, шлюз» — космос.

## 2. Бюджет кадра и хода — пункт 42

Вместо бесконечного «рисуем…» игрок видит честную картину:

```
0–2 с   кадр: ищем генератор…
3–7 с   кадр 5 с — генераторы молчат, рисуем локально      + бейдж «локальный фон»
8 с +   кадр 12 с — генераторы молчат, локальный фон уже в кадре
```

Секунды тикают в том же блоке, что и название, прогресс останавливается, как только
кадр пришёл (или пришёл отложенный — `scheduleLateFrame`). Провал больше не прячется:
бейдж остаётся с подписью «локальный фон — генераторы молчат».

## 3. Кэш кадров в IndexedDB — пункт 17

Появился `FrameStore` в `src/app.js`: кадр, полученный от генератора, складывается в
IndexedDB (24 записи, срок 21 день, подрезка по свежести), а при возврате в знакомое место
поднимается мгновенно и **без сети**.

Попутно вычищены две болезни:

* `blob:`-ссылки больше **не попадают в сохранение** — они не переживают перезагрузку,
  в сейв уходит только «долгая» ссылка, а показ идёт из хранилища телефона;
* возврат из сохранения сначала смотрит в IndexedDB и только потом дёргает сеть —
  после перезапуска по кадру **ноль сетевых запросов** (в сеть уходит только портрет героя).

---

## 4. Жесты — пункт 24

* **свайп влево по кадру** — нарисовать заново (новый запрос генератору, в книге — новый локальный фон);
* **свайп вверх по панели** — открыть историю, вниз — закрыть;
* **долгое нажатие на вариант** — объяснение шанса словами:
  `Почему 70%: РАЗ 1 против сложности 8 (Легко), +1 к броску.`
  Долгое нажатие гасит только собственный «щелчок», следующий осознанный тап по этому же
  варианту работает как обычно.

## 5. Режим «одной рукой» — пункт 27

Настройка **«Как держу телефон» → 👍 Одной рукой»**: варианты поднимаются к низу экрана,
минимальная высота кнопки 44 → **54 px**, текст варианта крупнее. Проверено по вычисленным
стилям, а не на глаз.

## 6. Доступность — пункт 26

* сцена объявляется как живая область (`role="status"`, `aria-live="polite"`);
* у каждого варианта есть подпись для скринридера: «текст. Сложность, шанс N процентов»;
* из модалок не выпускает **Tab** (фокус-ловушка), Esc закрывает диалог и пролог;
* играется с клавиатуры: **цифры 1–9** выбирают вариант, `U` — умение;
* смысл не держится на цвете: сложность и шанс продублированы словами и подписью,
  у варианта всегда есть иконка характеристики и число.

---

## 7. Проверки: что прогнано и что сказали прогоны

| Прогон | Итог |
| --- | --- |
| `node --test tests/` | **103 / 0** (в `tests/backdrop.test.js` — 8 новых: 24 основы, все места из плана, слои, детерминизм) |
| `npm run check` | ✓ (раскладки: 17 % / 22 % / 44 px, три размера текста) |
| `npm run smoke` | ✓ «все проверки пройдены» |
| `npm run pack` | ✓ «24 основы рисуются, слои видны», ночь темнее дня (21 / 32 по яркости), скриншоты `shots/v15-backdrop-pack.png`, `shots/v15-layers.png` |
| `npm run frame` | ✓ «БЮДЖЕТ КАДРА И ОФЛАЙН-КАДРЫ: всё на месте — ❤»: «кадр 5 с — генераторы молчат, рисуем локально», бейдж «локальный фон», после перезапуска кадр из хранилища, 0 запросов по кадру |
| `npm run touch` | ✓ «ЖЕСТЫ, ОДНА РУКА, ДОСТУПНОСТЬ: всё работает — ❤»: долгое нажатие объясняет шанс, свайп влево запросил кадр, свайп вверх открыл историю, цифра выбрала вариант, «одной рукой» — 54 px, Esc закрыл модалку |
| `npm run round` | ✓ «ФИНАЛ И РЕПЛИКИ: всё на месте» (реплика знакомого, .md, карточка) |
| `npm run round2` | ✓ «МИРЫ И ВТОРОЙ ШАНС: всё на месте» |
| `npm run offline` | ✓ «игра открывается без сети» (service worker: 1 кэш, 14 файлов) |
| `npm run screens` | ✓ «Все новые экраны на месте» (каналы, генераторы, 7 снимков) |

Сборка: `game.html` — **989 876 б ≈ 967 КБ** (была 935 882 б; пак фонов и жесты добавили текста кода,
картинок не добавили ни одной байтом).

Кадры-подтверждения в `shots/`: `v15-backdrop-pack.png` (все 24 основы с разными слоями),
`v15-layers.png` (время суток и погода на одном месте), `v15-frame-local.png` (честный прогресс
в игре), `v15-frame-restored.png`, `v15-long-press.png` (объяснение шанса), `v15-hand-one.png`
(режим одной руки).

---

## 8. Что осталось из плана

| Пункт | Почему ещё нет |
| --- | --- |
| 7–12 (раздел B: инвентарь 2.0, отношения со знакомыми, состояния, социальные броски, припасы, крит с последствиями) | не входили в выбранный вами объём; это отдельный крупный блок механик |
| 22 (чтение вслух из журнала) | вы исключали 22 из объёма |
| 37 (ежедневный забег) | не входил в выбранный объём; работает без ключа — можно взять следующим |
| остальное (1–6, 14–21, 23, 25, 28–36, 38–44) | сделано в прошлых отрезках, см. `PLAN.md` |

Ближайший очевидный шаг, если продолжаем: **37 (ежедневный забег на одном зерне, без ключа)**
или блок **B** (механики: предметы с эффектом, отношения, состояния).
