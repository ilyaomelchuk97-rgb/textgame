# v19 — шесть пунктов отзыва: настройки, озвучка, кадры, герой за мир, отряд, чистая папка

**Версия:** 19.0.0 · сборка `game.html` 1016 КБ · тесты 109/0 · кэш `sw.js` `dt2-v8`

---

## Коротко

| № | Просили | Что сделано | Чем проверено |
|---|---|---|---|
| 1 | «Какие бы настройки ни менялись — не выкидывать из настроек» | окно перерисовывается на месте; прокрутка, поле в фокусе и нажатая галочка возвращаются | `tools/ux-check.js`: прокрутка **189 → 189 px**, окно открыто, фокус остался в окне |
| 2 | «Озвучка отстаёт от прохождения ~на 2 раунда» | у сцены свой номер: старую цепочку чтения обрываем сразу, куски не дочитываются | тот же прогон: сцена **3 → 5**, кусков в голос 2, **старых после смены сцены — 0** |
| 3 | «Картинки информативнее: поднял записку — в руке записка с надписями» | действие разбирается на **предмет + ракурс**, предмет уходит в промпт и в ключ кадра | «Поднять записку…» → крупный план руки с запиской, метка `obj:записк`; в живом кадре ключ `…:obj:карт` |
| 4 | «Класс и раса, выбранные в своём мире, второй раз не спрашиваются» | выбор героя помнится за миром, шаги скрыты, есть строка «В этом мире уже выбрано» + «сменить» | второй заход: разделы **0/3**, строка «Воин · Человек · Улицы предместий» |
| 5 | «Персонажи на главном экране должны меняться, минимум 10 фигур» | **14 фигур** (7 героев + 7 монстров), пара новая каждый заход в меню | `tools/roster.js`: за 6 заходов 9 разных фигур, за 8 — 12; сдвиг отряда 334 px за 1.2 с |
| 6 | «Пересмотреть workspace, удалить мусор» | убраны отработавшие патчи, пробы и старые снимки прогонов | папка похудела на 15 МБ и 100+ файлов, тесты и живые прогоны после уборки зелёные |

---

## 1. Настройки больше не выкидывают

**Что было.** Переключение галочки пересобирало окно целиком: игрок терял место в длинном списке,
а поле ввода ключа — фокус. Хуже: после ответа сервера (`/api/health`) список каналов обновлялся сам
и стирал кнопку, на которую игрок только что нажал, — окно «мигало» и прыгало.

**Что стало.**
* Окно настроек перерисовывается **на месте**: `openSettings({ keep: true })` — 18 мест, включая
  кнопку звука. Перед перерисовкой запоминается прокрутка и то, что было в фокусе (текстовое поле —
  по подсказке, кнопка — по подписи), после — возвращается.
* Строка каналов (`choiceRowLive`) сама чинит фокус: если игрок нажал «Отклик» или «Авто (умный)»,
  то после автообновления списка фокус и место остаются на той же кнопке.
* Выбор канала и генератора переключает подсветку без пересборки — окно не мигает.

**Замер** (`node tools/ux-check.js`): галочка «Авто (умный, быстрый)» → окно открыто, прокрутка
`189 → 189 px`, фокус в окне: да. Снимок — `shots/v19-settings-stay.png`.

## 2. Озвучка догоняет экран

**Что было.** Абзацы читались цепочкой, и цепочка не отменялась: пока читается старая сцена, новая
ждёт. Плюс остановка глушила mp3 без события «конец» — очередь висела до страховочных 30 с.

**Что стало.** У каждой сцены свой номер (эпоха). Новая сцена открывает новую эпоху, все куски старой
сразу считаются прочитанными, страховочный таймер больше не тянет время. Реплика знакомого тоже
проверяет, к той ли сцене относится.

**Замер:** сцена 3 → 5, кусков ушло в голос 2, кусков старой сцены после ухода вперёд — **0**.

## 3. Кадр показывает то, что случилось

**Что было.** Кадр рисовал место, а не действие: поднял записку — получил ту же комнату.

**Что стало.** Действие разбирается на предмет и ракурс (`FOCUS_ART` — 34 предмета, `FRAMING_ART` —
8 глаголов):

| Игрок написал | Кадр |
|---|---|
| поднять записку, читать письмо | крупный план: `a crumpled handwritten note held in a gloved hand, legible ink letters on the paper` |
| идти, возвращаться | общий план движения: `wide travelling shot…` |
| осмотреться, оглянуться | обзор местности: `wide establishing shot…` |
| ждать, слушать | кадр **не меняется** — место то же |

Предмет попадает и в ключ кадра (`place:obj:записк`), поэтому новый ракурс не подменяется старым
кадром комнаты из кэша. В живом прогоне видно ключ `cosmos:караульная-у-моста:obj:карт`.

## 4. Класс и вид спрашиваются один раз на мир

Выбор героя (класс, вид, происхождение, имя, титул) запоминается за миром. Во второй заход в тот же
мир шаги создания скрыты, а сверху висит строка «**В этом мире уже выбрано:** 🛡️ Воин · 🧍 Человек ·
Улицы предместий» и кнопка «сменить» — игрок сам решает, переигрывать героя или нет. В мире, где
подходит только один вариант, раздел не показывается вовсе.

**Замер:** первый заход — разделы 3/3 открыты, второй — 0/3, строка на месте, выбор сохранён
(`warrior · human · streets`). Снимок — `shots/v19-hero-pick-kept.png`.

## 5. Отряд главного экрана: 14 фигур

| Герои | Монстры |
|---|---|
| Рыцарь, Космо-инженер, Кибер-самурай, Ассасин, Тёмный маг, Орк-воин, Пират | Дракоша, Некроморф, Каменный голем, Ледяной волк, Дрон-охотник, Призрак, Зомби |

* Пара **меняется при каждом заходе в меню** — прошлые двое в новую пару не попадают.
* Фигуры бегут через экран друг за другом (погоня), останавливаются вне меню, при касании
  подпрыгивают и говорят: «Рыцарь: „Я его не звал!“», «Дрон-охотник: „Цель найдена“ (жужжит)».
* Нарисованы кодом (SVG), без картинок из сети: работают офлайн и весят килобайты.
* Обзорный лист всех 14 фигур — `shots/v19-roster-sheet.png`, пары на телефоне —
  `shots/v19-roster-1.png`, `-2.png`, `-3.png`.

**Замер:** за 6 заходов в меню показано 9 разных фигур, за 8 — 12; сдвиг отряда 334 px за 1.2 с.
Попутно исправлен дефект: пара менялась «через раз» — теперь ротация на каждом заходе.

## 6. Рабочая папка прибрана

| Убрано | Почему |
|---|---|
| `tools/patch-*.py` — 14 скриптов | одноразовые патчи, уже применённые к коду; повторять их нельзя |
| `tmp/` — 70 файлов проб | черновые скрипты и логи живых проверок прошлых раундов |
| `images/` — 4 файла | черновые пробы картинок |
| `shots/` — 39 снимков | кадры прогонов (`01-menu…16-hero`), отладочные (`dbg-*`), старые версии до v13 |
| `samples/` — 4 файла | неиспользуемые образцы голоса (отдельные дорожки мультиязычных голосов) |

Осталось нужное: код (`src/`), сборка (`game.html`), сервер, тесты, живой набор прогонов в `tools/`,
отчёты, образцы голоса из README и снимки, на которые ссылаются отчёты. Папка похудела
**610 → 595 МБ** (остальное — `node_modules` и браузер Playwright, они вне снапшота), файлов стало
меньше на сотню. После уборки `npm test` — 109/0, смоук и живые прогоны — зелёные.

---

## Приёмка: что прогнано на итоговой сборке

| Проверка | Результат |
|---|---|
| `npm test` | **109/0** (движок 88, книги 6, фон 7, отряд героев 8) |
| `npm run check` | синтаксис ✓, тесты ✓, раскладка **5/5** (шапка 14–17 %, кадр 22 %, кнопки ≥44 px) |
| `npm run smoke` | весь путь игрока ✓, отряд `pirate + wolf`, сдвиг 334 px за 1.2 с, ошибок консоли нет |
| `tools/ux-check.js` | четыре обещания v19 — ❤ |
| `tools/roster.js` | 14 фигур, пары меняются, бегают — ❤ |
| `round`, `round2`, `offline`, `touch`, `frame`, `pack`, `screens`, `fonts`, `cloud` | ✓ (облачный сейв × интерфейс работает, кадр приходит, 24 основы фона рисуются) |
| `mistral`, `glm`, `hf` (моки каналов) | ❤ — агент ведёт игру, бесплатная flash ведёт игру, HF рисует кадр с ключом |
| `turns:live` (живой Mistral-агент) | ❤ — 2 хода, средний **4.4 с**, сцена логична, варианты со шансами |
| `hf:live` (живой Hugging Face) | кадры `flux-merged` 5.2 с ✓; **текст — 401: токен «textgame» протух** (см. ниже) |

## Одна неприятность, которую видно честно

Токен Hugging Face, который вы давали в v18, **истёк**: `HTTP 401 «User Access Token „textgame“ is expired“`.
Игре от этого не больно — канал сразу отказывается и ход ведёт другой мастер, причина видна в
`/api/health`; картинки через Space'ы (`flux-merged`) при этом рисуются. Когда получите свежий токен
`hf_…`, вставьте его в «Настройки → Канал ИИ» (или в переменную `HF_API_KEY` на сервере) — канал
заработает без пересборки.

## Что дальше

По плану остаётся выбор: **блок B «Механики и глубина»** (предметы с эффектами, отношения со
знакомыми, состояния, социальные броски, припасы, крит с последствиями) или **37 «Ежедневный забег»**
(один seed на всех, встроенный мастер, работает без ключа). Пункт 22 исключён вами ранее.
Скажите, что берём.
