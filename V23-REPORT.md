# Кости и Судьбы — отчёт за раунд v23: «Забег дня»

24 сентября 2026 · сборка `game.html` — 1260 КБ (1 289 775 Б) · тесты **142/142** · прогон забега дня — **58 проверок**, всё в живом браузере на iPhone 13 mini

---

## 1. Что просили

Из `PLAN.md`, пункт 37 (последний открытый из непокрытых):

> **Ежедневный забег.** Один `seed` на всех, одинаковый мир и броски, встроенный мастер — то есть
> работает **без ключа**. Результат сохраняется локально, показывается «мой лучший» и «сколько
> прошли другие» (если появится облако). Лучший способ вернуть игрока завтра.

Облако в игре уже есть (сейвы по коду), поэтому «сколько прошли другие» сделано сразу, а не
отложено: уходят только числа.

---

## 2. Что сделано

### 2.1. Вход и экран дня

В главном меню третья кнопка — **«🗓 Забег дня»** (со значком: сколько вы набрали сегодня, если уже
играли). Она открывает свой экран, где до старта видно всё:

* **код дня** из четырёх знаков без похожих букв («7QPJ») и дата — по ним видно, что забег тот же,
  что у других;
* **мир дня** — карточка с обложкой, жанром и подписью (например, «Пустошь Кар-Ата», жанр
  «Постапокалипсис»);
* **герой дня** — класс, раса, происхождение и умение, заданные забегом;
* **цель дня** — одна из двенадцати: «Договориться с тем, с кем договориться нельзя», «Вынести
  оттуда то, что держат силой», «Найти вход, о котором не знает никто» и так далее;
* **мой результат** — сегодняшний, лучший за всё время (с датой) и серия «🔥 Дней подряд: 3»;
* **другие игроки** — «👥 прошли день: 12 героев · цель взяли: 4 · лучший счёт: 410 · средний: 236 ·
  вы — 5-й», а если облака нет — честное «облако молчит, результат сохранён на телефоне»;
* **правила** пятью строками — чтобы режим не требовал объяснений;
* внизу кнопка **«🗓 Начать забег дня»**, а если сегодняшний забег не закончен — **«▶ Продолжить
  забег дня»** (нажатие возвращает в ту же кампанию на тот же ход).

### 2.2. Один мир и одни броски на всех

* **Дата → зерно → день.** Из ключа дня (`2026-09-24`) считается зерно, из зерна — сценарий, класс,
  раса, происхождение и цель дня. Ничего не «допрашивается» у ИИ: подборы выводятся из даты, поэтому
  у всех, кто открыл игру в тот же день, планета одна.
* **У кубика теперь собственный поток случайностей.** Это важная правка механики: раньше броски шли
  из общего генератора, и на них могли влиять посторонние вещи (подбор промпта кадра, случайная
  реплика) — тогда «одни броски у всех» ломались бы. Теперь `E.rnd.setDiceSeed(seed)` двигает только
  кубик, всё остальное остаётся вольным. Проверяется отдельно: между двумя замерами бросков
  подбрасывается сотня посторонних случайностей — броски совпадают.
* **Проверка выполняется в живом браузере:** шесть бросков подряд из зерна дня воспроизводятся
  один в один, другой день даёт другой мир, цель и броски.

### 2.3. Судьба дня вместо шагов выбора

Класс, раса и происхождение на экране героя **не спрашиваются** — они выбраны за игрока и подписаны:
«🗓 Забег дня: мир, класс, раса и происхождение заданы судьбой — ваше имя и решения». Шагов выбора
нет, кнопки «другой набор» нет, строки «уже выбрано · сменить» тоже нет (она бы позволила обойти
судьбу дня). Иначе сравнивать результаты было бы нечестно. Дальше — обычная игра: печать текста,
кубик с анимацией по выпавшему значению, варианты с шансами, журнал, сумка, знакомые.

Игре забега добавляется **цель дня** (она же цель кампании) и зерно — мастер видит в промпте блок
«ЗАБЕГ ДНЯ 7QPJ: цель дня … держись именно этой цели». Вопрос «с чего начнём» в забеге не задаётся:
цель уже названа.

В шапке игры вместо главы видно **«🗓 забег дня · Ход 7 · ❤️ 5/12 · 🎒 6»**, в журнале — плашка
забега: код, цель дня, текущий счёт и время до нового забега.

### 2.4. Работает без ключа и без сети

Ведущий — встроенный мастер (Mistral/GLM, как и в обычной игре), своего ключа не нужно. Отдельно
проверено худшее: **сеть мертва целиком** (ни `/api/*`, ни прямых каналов) — забег всё равно
играется локальным мастером по сюжетным вехам, доходит до цели дня за 8 ходов и закрывается финалом
со счётом.

### 2.5. Счёт, который можно объяснить

В финале забег показывает не одно число, а весь расклад:

| Слагаемое | Сколько |
|---|---|
| 🏁 цель дня взята / финал без цели дня | +120 / +30 |
| ❤️ жизнь на финале | +6 за пункт |
| 🎒 припасы | +4 за каждый |
| 🧰 предметы | +5 за каждый |
| 🌟 криты | +12 за каждый |
| 💀 провалы | −5 за каждый |
| ⏱ темп | +40 − 2 за ход (быстрее — больше) |

Пример из прогона: `🏁 +120, ❤️ +30, 🎒 +24, 🧰 +5, 💀 −20, ⏱ +24 → 🏆 итог дня: 183 очка · цель дня
взята`. Рядом — «это и есть ваш лучший результат» или «лучший за всё время: 340 (12 сентября)».

### 2.6. Своё и чужое: только числа

* **На телефоне** результат дня живёт в `dt2:daily` (в один день хранится последний забег, история
  дней подрезается до 60) плюс отметка незаконченного забега `dt2:dailyRun` — из неё растёт кнопка
  «Продолжить».
* **В облако** уходит ровно четыре числа и код дня: дата, очки, ходы, взята ли цель. Ни имени,
  ни сейва, ни текста истории — в правилах это сказано прямо.
* **Сервер** получил `GET /api/daily?date=ГГГГ-ММ-ДД` (сколько прошли, лучший, средний, сколько
  взяли цель, ваше место) и `POST /api/daily` (принять результат и сразу вернуть место). Хранилище —
  `data/daily-runs.json`, срок 60 дней, до 2000 забегов в день и 120 дней в файле. Мусор отсекается:
  дата формата `ГГГГ-ММ-ДД` обязательна (иначе 400), очки обрезаются до 100 000, ходы — до 4000.

---

## 3. Что проверено

**Логика — `node --test tests/`:** 142/142, из них 16 новых (`tests/daily.test.js`): ключ дня и
полночь, seed и код дня, воспроизводимость мира/героя/цели, поток кубика и его независимость от
посторонней случайности, вольный кубик без зерна, счёт по слагаемым (цель дороже всего, темп,
провалы), доска (место, средний, лучший), запись дня и подрезка истории, лучший результат и серия.

**Живой браузер — `node tools/daily.js` (58 проверок, iPhone 13 mini, три части):**

1. **Экран и облако** — кнопка в меню открывает экран дня; код дня без похожих знаков; мир, герой,
   цель и пять правил на месте; главные кнопки 44+ px, значки шапки как на других экранах (38 px);
   облако отвечает «прошли день: 12»; мир/герой/цель из даты воспроизводятся один в один; шесть
   бросков из зерна совпадают; подбор кадров броски не сдвигает; другой день — другой день;
   на экране героя шагов выбора нет, «другой набор» отсутствует, попытка перебора ничего не меняет;
   тема «Лёд» доходит до нового экрана (кнопка обледенела).
2. **Полный забег без сети** — тот же день, ноль запросов к ИИ: цель дня становится целью кампании,
   шапка помечена забегом, кубик из зерна, вопрос «с чего начнём» не задаётся, в журнале плашка
   забега, забег можно закрыть и **продолжить** с того же хода, игра доигрывается до цели дня,
   в финале — счёт слагаемыми и честное «облако молчит», результат остаётся на телефоне, после
   финала кубик снова вольный, кнопка «к забегу дня» возвращает на экран дня с сегодняшним
   результатом и лучшим, а в меню на кнопке видно сегодняшние очки.
3. **Облако по-настоящему** — обмен с живым сервером: пустой день отдаётся нулями, результат принят
   и посчитан (лучший, средний, место 1), повторный запрос дня видит мой забег, мусорная дата — 400,
   неправдоподобные очки обрезаны до 100 000 и подняли лучший, доска считает все заходы дня.
   Проверочные дни убираются из `data/daily-runs.json` до и после прогона.

**Регрессы сданного (не сломано):** `npm run smoke` ✓ · `layout` 5/5 (шапка 14 %, картинка 22 %,
кнопка 44 px) ✓ · `skins` — девять материалов ✓ · `v20` — блок B и интерфейс ✓ · `books` — истории
и фильтр жанров ✓ · `touch` — жесты, «одна рука» (54 px), доступность ✓ · `offline` — игра открывается
без сети ✓ · `cloud` — облачный сейв из интерфейса ✓ · `frame` — бюджет кадра и кадр из IndexedDB ✓.
Плюс отдельно проверен вход через `index.html` (разработка): экран дня открывается, ошибок нет.

Снимки: `shots/v23-daily.png` (экран дня), `v23-daily-hero.png` (судьба дня), `v23-daily-end.png`
(финал со счётом), `v23-daily-after.png` (экран дня после забега), `v23-daily-menu.png` (счёт на
кнопке меню), `v23-daily-ice.png` (забег в теме «Лёд»).

---

## 4. Что изменилось в файлах

| Файл | Что |
|---|---|
| `src/daily.js` | **новый**: дата → seed → мир/герой/цель дня, код дня, счёт слагаемыми, доска, лучший забег и серия. Чистые функции: ни DOM, ни сети |
| `src/engine.js` | у кубика свой поток случайностей (`rnd.setDiceSeed/clearDiceSeed/diceSeeded`), генератор с зерном, блок «ЗАБЕГ ДНЯ» в описании мира для мастера |
| `src/app.js` | экран и запуск забега, судьба дня на экране героя, счёт и запись результата, плашка в журнале, пометка в шапке, итог в финале, «ём ещё попытка», роутер и значок в меню |
| `src/api.js` | `dailyBoard` и `dailySubmit` — обмен с облаком дня |
| `server.js` | `GET/POST /api/daily`, хранилище `data/daily-runs.json`, срок 60 дней, защита от мусора |
| `index.html` | кнопка «🗓 Забег дня» в меню и экран `#screen-daily`, подключён `src/daily.js` |
| `src/skins.css` | мелочь: значок на кнопке берёт цвет её текста (был битый `var(..., --sk-btn-muted)`) |
| `tests/daily.test.js` | **новый**: 16 тестов забега дня |
| `tools/daily.js` | **новый**: 58 проверок в браузере (три части) |
| `build.py`, `sw.js` | модуль `daily.js` в сборке; кэш `dt2-v12` + файл в оболочке |
| `package.json` | версия 23.0.0, скрипт `npm run daily`, добавлен в `checkall` и `check` |
| `README.md` | раздел «Что нового в v23», структура, тесты, прогон |

Мелкие правки по пути: в забеге дня не показывается строка «в этом мире уже выбрано · сменить»,
перебор вариантов героя отвечает «в забеге дня герой задан судьбой», а вопрос «с чего начнём» не
задаётся — цель дня уже названа.

---

## 5. Что осталось

Из плана больше нет непроверенных пунктов: **17** (кэш кадров в IndexedDB) и **42** (честный прогресс
кадра) подтверждены прогонами этого раунда — `npm run frame` поднимает кадр из хранилища без сети,
а строка статуса честно говорит «кадр 6 с — генераторы молчат, рисуем локально».

Не сделано сознательно:

* **41 (ленивая сборка `game.html`)** — картинки по-прежнему внутри файла: `game.html` остаётся одним
  файлом без зависимостей (это записано в плане как сильная сторона). Если нужно экономить первый
  заход на 3G — это отдельная правка со сменой принципа, её стоит заказывать явно.
* **39 (расширить набор игровых сеттингов)** и **40 (библиотека своих миров: «тем же миром новым
  героем»)** — сейчас режимы по играм дают Азерот, Найт-Сити и Руны, а сохранённый мир можно
  загрузить; добавление 5–8 новых сеттингов и кнопки «сыграть тем же миром новым героем» — это
  контент без ИИ, следующий очевидный шаг.
* **13 (пак офлайн-фонов из 12–16 основ со слоями)** — процедурный фон есть и работает при мёртвых
  генераторах, но до пакета «каньон, ночной рынок, станция, порт, таверна + время суток, погода,
  туман, костёр» его объём не доводился.

---

## 6. Дополнение: у тем появился настоящий материал (v23.1)

Замечание заказчика: «оформление выглядит очень плохо, что ни выбери — лёд или закат, всё плохо».
Причина была в том, что темы рисовались **только градиентами CSS** — плоско и почти без различий.

Что сделано:

* **Материал тем — сгенерированные картинки**: фон экрана (640×1148) и фактура кнопок и блоков
  (448×448) для восьми тем: Лёд, Закат, Киберпанк, Ночь, Терминал, Материальная, Пергамент,
  Тушь и медь. «Чёрная» без картинки намеренно — это тема «только контур».
* **Фон темы теперь виден**: раньше его перекрывало затемнение почти в ноль — теперь у каждой темы
  три слоя затемнения (фон, полосы, панель текста), и они подобраны так, чтобы текст читался.
* **Фактура легла на кнопки, варианты, вкладки, чипы, карточки, окна настроек, пролог и эпилог**,
  а также на образцы тем в настройках — образец показывает тот же материал.
* **Починено по ходу**: у «Терминала» центральная вспышка фосфора съедала текст (притушена
  радиальной маской), у «Туши» кованая медь рябила под текстом (размыта и приглушена,
  плитка крупнее), у «Ночи» фактура была почти чёрной (поднята яркость).

Цена: `game.html` вырос с 1250 до **1737 КБ** (+487 КБ, из них 352 КБ — шестнадцать WebP-материалов,
вшитых в CSS). Требование «один файл без зависимостей» сохранено. Мастера PNG лежат в
`assets/themes/_src/`, промпты записаны в `tools/themes.py` — материал можно пересобрать другой или
заменить, не трогая интерфейс.

Проверено: `npm run skins` — материал есть у 8 тем из 9, доходит до кнопок (8/8), надпись на
материале читается (худший средний контраст 9.8:1); `npm test` 142/142; регрессы `smoke`, `layout`
(шапка 14 %, картинка 22 %, кнопка 44 px), `v20`, `books`, `daily` (58/58), `touch`, `offline`,
`frame` — без замечаний. Общий снимок: `shots/v23-themes-art.png`.
