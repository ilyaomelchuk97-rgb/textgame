# Отчёт v20 — механики и глубина, новый интерфейс

Сборка: `game.html` **1 073 КБ** (1 098 891 байт), версия проекта **20.0.0**, service worker `dt2-v9`.
Секретов в сборке нет (живых ключей — 0 вхождений).

---

## 1. Вшитый ключ Hugging Face удалён

| Было | Стало |
|---|---|
| `HF_BUILTIN_KEY = 'hf_vaOY…'` прямо в `server.js` | только из окружения: `HF_API_KEY` / `HUGGING_FACE_TOKEN` / `HF_BUILTIN_KEY` |
| подсказка «ключ вшит в игру» | «нужен свой ключ `hf_…`», поле в настройках остаётся |
| — | `/api/health` показывает `hf.key`: `окружение` / `сервер` / `нет` |

* `grep hf_vaOY` по проекту — **0 вхождений**.
* Канал Hugging Face и своё поле ключа остались, живые проверки: `npm run hf` → ход на
  `zai-org/GLM-5.3-Flash`, поток 5 частей, кадр 12 420 байт, пустые кредиты → отказ за 47 мс.
* README: раздел HF переписан («Ключ — только свой»), пометка года исправлена на v20.

---

## 2. Блок B «Механики и глубина» — сделан целиком (пп. 7–12)

### Движок (`src/engine.js`), схема сейвов 3 → 4

| Пункт | Что сделано |
|---|---|
| **7. Инвентарь 2.0** | Предмет = имя + эффект: `heal` (+здоровье), `boost` (+power к своей проверке на один ход), `advantage` (два d20), `key` (открывает следующую веху арки), `trophy` (память). У каждого — иконка, глагол («Выпить», «Приготовить», «Открыть») и строка «что делает». Мастер выдаёт предмет полем `effects.give {name, kind, power, stat}` |
| **8. Отношения** | `RELATIONS`: друг (договариваться на 4 легче), должник (помощь раз за кампанию), нейтрально, враг (на 4 труднее, зато подозревать его — с преимуществом). Доверие 0–5, `setTrust` пересчитывает отношение |
| **9. Состояния** | Рана −1 (сила/телосложение/ловкость), усталость −1 (ум/восприятие/воля), вдохновение +1 (всё), «на мушке» −1 (ловкость/восприятие). 2–4 хода, тикают вместе с перезарядкой умения, попадают в подсказку мастеру |
| **10. Соцбросок против знакомого** | `socialPlan` узнаёт знакомого в тексте действия в любом падеже («уговорить **Власа**», «попросить стражника **Грома**»), считает поправку к сложности и преимущество. Броском пользуется и кнопка: **шанс на кнопке = шанс в броске** |
| **11. Припасы 6 из 8** | Привал (−1: +2 здоровья, снять усталость), перевязка (−1: +3 здоровья, перевязать рану), обход опасного места (−1: преимущество). Ниже нуля не уходят |
| **12. Крит и провал** | Крит «это заметили»: нить в память кампании + 1 доверия знакомому. Провал «дороже»: сначала припас, если припасов нет — усталость. Работает и во встроенном мастере, без ключа |

Плюс: `mechanicBlock` уходит мастеру в каждом ходовом промпте (припасы, состояния, предметы с действием,
знакомые с доверием, правила крита и провала); `heroDescription` показывает припасы и состояния;
`migrate` поднимает старые сейвы (строки инвентаря → предметы с эффектом, припасы и состояния добираются,
вещи не теряются).

### Интерфейс (`src/app.js`, `index.html`, `src/styles.css`)

* **Строка механик** над вариантами действий: чипы состояний и то, что правда пригодится сейчас —
  предмет с эффектом («🧪 Выпить: настойка лекаря»), «🔥 Привал», «🩹 Перевязка», зов должника.
  Нет нужды — строки нет. Ушла за край — правый край мягко тает (видно, что листается).
* **Припасы всегда на виду** в шапке рядом с ходами: `Пролог · Ход 1 · ❤️ 10/15 · 🎒 6`.
* **Журнал стал сумкой**: карточки предметов (иконка · название · «что делает» · кнопка использования),
  полоска припасов, раздел состояний с остатком ходов, список знакомых с отношением и доверием
  (●●●●○), у должника — кнопка «🤝 Позвать».
* Кнопка «использовать» работает и в бою, и вне боя; ключ двигает веху арки и говорит об этом.

---

## 3. Интерфейс пересмотрен (пункт 3 запроса)

* **Девять тем + «Авто»** в «Настройки → Оформление»: **Материальная** (светлая, Material — база),
  Ночь, **Киберпанк**, Терминал, Пергамент, Тушь и медь, Закат, Лёд, Чёрная (OLED).
  Тема задаёт не только цвета, но и подложки, радиусы и тени (`--bar-bg`, `--btn-bg`, `--card`, `--wash`),
  поэтому светлые темы выглядят светлыми, а не выключенными тёмными. «Авто» берёт тему по жанру кампании.
* **Все кнопки ровные.** Нижний ряд — кнопки-значки одинаковой ширины: разброс **0 px**, высота **44 px**
  (История · Сумка · Журнал · Карта · Забег · Пролог · Озвучка). Кнопки вариантов: одна высота 44 px,
  сложность и шанс в одном чипе фиксированной ширины, стат — во втором, метка больше не переносится.
* **Раскладка цела:** шапка 14 % (не ниже 90 px на коротких экранах, 15 % на 13 mini),
  картинка 22 %, тексту ≥23 % экрана, пустоты внизу нет — на всех пяти устройствах.
* Сборка выросла до 1 073 КБ (была 1 016 КБ): девять тем, механики и карточки.

---

## 4. Проверено

| Прогон | Результат |
|---|---|
| `npm test` | **119/119** (движок 88, **механики блока B 10**, книги 6, фон 7, силуэты 8) |
| `npm run layout` | **5/5**: 13 mini, SE, 15 Pro Max, Pixel 5, 360×640 — шапка 14–15.8 %, картинка 22 %, кнопка 44 px |
| `npm run v20` | **19/19**: ряд значков ровный (0 px), припасы в шапке, чипы состояний, настойка 10 → 15, ключ «открывает путь», карточки предметов, полоска припасов, знакомый-должник, 10 тем, Материальная 0.95 / Киберпанк 0.03, игра продолжается в выбранной теме |
| `npm run smoke` | весь путь игрока — ✓, ошибок страницы нет |
| `node tools/ux-check.js` | четыре обещания v19 целы (настройки, озвучка, кадр по предмету, выбор героя за миром) |
| `npm run touch`, `npm run frame` | жесты, «одна рука», клавиатура — ✓; кадр приходит |
| `npm run hf`, `npm run glm`, `npm run mistral` | все три канала ведут игру и рисуют кадры |
| Снимки | `shots/v20-game.png`, `v20-bag.png`, `v20-themes.png`, `v20-material.png`, `v20-material-bag.png`, `v20-theme-neon.png` |

Новые тесты: `tests/mechanics.test.js` (10) — предметы и их эффекты, использование, состояния,
припасы, отношения и соцбросок, крит и цена провала, применение эффектов хода, контракт мастера,
подъём сейва схемы 3 → 4.
Новый прогон: `tools/v20.js` (`npm run v20`).

---

## 5. Что осталось (не из этой задачи)

* **п.37 «Ежедневный забег»** — открыт, не отменялся (п.22 исключён ранее).
* **`npm run cloud-check`** — падает с `npm error` (debug-0.log), причина не разобрана.
* **`frame.js` и IndexedDB** — прогон по-прежнему пишет «после перезапуска кадр пошёл в сеть,
  из хранилища не поднялся»; разбор отложен.
* Мелочи на приглядку: иконки предметов — эмодзи (в песочнице нет эмодзи-шрифта, на iPhone они
  отрисовываются штатно); кнопка-значок «Пролог» появляется только у первой кампании.
